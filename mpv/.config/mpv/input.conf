# ⏯️ Play/Pause with Spacebar
SPACE cycle pause          # Hit SPACE to toggle play/pause

# ⏩ Seek Forward/Backward (5 seconds)
RIGHT seek 5 exact
LEFT seek -5 exact

# 🔊 Volume Control (5% increments)
UP add volume 5            # Increase volume by 5%
DOWN add volume -5         # Decrease volume by 5%

# 🖥️ Fullscreen Toggle
f cycle fullscreen         # Press 'f' to toggle fullscreen

# 🛑 Stop Playback
q stop                     # Press 'q' to stop the media

# 🎥 Cycle Subtitle Track
v cycle sub                # Cycle through subtitle tracks

# 🎧 Cycle Audio Track
b cycle audio              # Cycle through available audio tracks

# Bind custom shortcut to show time
t show-text "Time: ${time-pos} / ${duration}"
